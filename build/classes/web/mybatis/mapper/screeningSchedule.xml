<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="screeningSchedule">
	<insert id="insertScreen" parameterType="web.mybatis.vo.ScreeningScheduleVO">
		INSERT INTO screening_schedule(movieCd, t_name, ss_time, ss_date)
		VALUES(#{movieCd}, #{t_name}, #{ss_time}, #{ss_date})
	</insert>
</mapper>